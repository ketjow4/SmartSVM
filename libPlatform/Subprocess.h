#pragma once

#include <string>
//#include "Windows.h"

namespace platform::subprocess
{
//std::pair<std::string, DWORD> launchWithPipe(const std::string& command);

//int launch(const std::string& command);
} // namespace phd::platform::subprocess
